plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE, mar = c(3, 3, 3, 10))
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 1), add = TRUE, )
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE, mar = c(3, 3, 3, 3))
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 1), add = TRUE, )
# Add vector overlays
lines(munich_poly, col = "black", lwd = 2)
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE, mar = c(3, 3, 3, 5))
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 1), add = TRUE, )
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE, mar = c(3, 3, 3, 1))
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 1), add = TRUE, )
# Add vector overlays
lines(munich_poly, col = "black", lwd = 2)
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE)
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 2), add = TRUE)
# Add vector overlays
lines(munich_poly, col = "black", lwd = 2)
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE)
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.2, ncol = 2), add = TRUE)
# Add vector overlays
lines(munich_poly, col = "black", lwd = 2)
plot(r_cropped, alpha = 0.6, main = "GOSIF and Crop Type Overlay", legend = FALSE)
# Plot the second raster on top
# (Adjust its alpha if needed, or leave at 1 to see it clearly)
plot(crop_cropped_wgs84, col = palette_crop,
plg = list(cex = 0.4, ncol = 1), add = TRUE)
# Add vector overlays
lines(munich_poly, col = "black", lwd = 2)
points(munich_coords, pch = 3, col = "red", lwd = 2)
text(11.576, 48.15, "Munich", col = "red", pos = 3)
sif <- readRDS('../data/oco2_sif.rds')
View(sif)
unique(sif$Delta_date)
hist(sif$Daily_SIF_740nm)
sif <- readRDS('../data/oco2_sif.rds')
sf::st_crs(sif)
sif <- readRDS('../data/oco2_sif.rds')
unique(sif$Delta_date)
sif <- readRDS('../data/oco2_sif.rds')
arrow::write_parquet(sif, '../data/oco2_sif.parquet')
sif_df <- sif |> sf::st_drop_geometry()
arrow::write_parquet(sif_df, '../data/oco2_sif.parquet')
nuts3 <- giscoR::gisco_get_nuts(year = "2021", epsg = 4326, nuts_level = 3, resolution = "01", country = "DE")
nuts3_bavaria <- nuts3  |> filter(startsWith(NUTS_ID, "DE2"))
library(tidyverse)
nuts3_bavaria <- nuts3  |> filter(startsWith(NUTS_ID, "DE2"))
View(nuts3_bavaria)
saveRDS(nuts3_bavaria, './nuts3.rds')
View(nuts3_bavaria)
sf::st_write(
nuts3_bavaria,
"my_geometry.gpkg",
layer = "my_layer",delete_layer = TRUE
)
library(terra)
library(sf)
library(tidyverse)
library(giscoR)
nuts1 <- gisco_get_nuts(year = 2021,  resolution = "10", nuts_level = 1, epsg = 4326)
bavaria_sf <- subset(nuts1, NUTS_ID == "DE2")
bavaria_v  <- vect(bavaria_sf)
# gosif raster
gosif_202405 <- rast('../data/enhanced_sif/gosif/GOSIF_2024.M05.tif')
# necessary transformations to correct sif value's range
gosif_202405[gosif_202405 >= 32766] <- NA
gosif_202405 <- gosif_202405 * 0.0001
gosif_bav_crop <- crop(gosif_202405, bavaria_v)
gosif_bav_crop <- mask(gosif_bav_crop, bavaria_v)
# raster has different fields in germany marked, colored (winter wheat, maize, soyabean etc.)
crop_type <- rast('../data/crop_type_tif/croptypes_2024.tif')
# set levels for crop type raster
crop_classes <- readr::read_delim("../data/crop_type_tif/LEGEND_CropTypes.txt", delim = "\t")
colnames(crop_classes) <- c("code", "label")
levels(crop_type) <- data.frame(value = crop_classes$code, crop = crop_classes$label)
bavaria_utm <- project(bavaria_v, "EPSG:32632")
# Crop the Germany-wide crop raster down to Bavaria first
crop_type_bav <- crop(crop_type, bavaria_utm)
crop_type_bav <- mask(crop_type_bav, bavaria_utm)
# Project the (already Bavaria-masked) SIF raster to UTM
# (continuous values -> bilinear is appropriate)
gosif_bav_utm <- project(gosif_bav_crop, "EPSG:32632", method = "bilinear")
# make each sif pixel into polygon
zones <- as.polygons(gosif_bav_utm, dissolve = FALSE, values = TRUE, na.rm = TRUE)
# Project the (already Bavaria-masked) SIF raster to UTM
# (continuous values -> bilinear is appropriate)
gosif_bav_utm <- project(gosif_bav_crop, "EPSG:32632", method = "bilinear")
# (optional) ensure it’s still strictly within Bavaria border after reprojection
gosif_bav_utm <- mask(gosif_bav_utm, bavaria_utm)
# make each sif pixel into polygon
zones <- as.polygons(gosif_bav_utm, dissolve = FALSE, values = TRUE, na.rm = TRUE)
zones$zone_id <- 1:nrow(zones)
nrow(zones)
zones
zone_r <- rasterize(zones, crop_type_bav, field = "zone_id", touches = TRUE)
tab <- terra::crosstab(zone_r, crop_type_bav, long = TRUE)   # columns like: zone_id, crop, freq
compareGeom(zone_r, crop_type_bav, stopOnError = TRUE)
tab <- crosstab(zone_r, crop_type_bav, long = TRUE)   # columns like: zone_id, crop, freq
compareGeom(zone_r, crop_type_bav, stopOnError = TRUE)
s <- c(zone_r, crop_type_bav)
names(s) <- c("zone_id", "code")
s
# Frequency table of unique combinations
tab <- freq(s, useNA = "no")
?freq
# Frequency table of unique combinations
tab <- freq(s, wide = FALSE)
head(tab)
plot)zone_r)
plot(zone_r)
View(tab)
plot(crop_type_bav)
head(tab)
# Frequency table of unique combinations
tab <- freq(s, bylayer = FALSE)
head(tab)
zone_r
plot(zones)
s <- c(zone_r, crop_type_bav)
names(s) <- c("zone_id", "code")
install.packages("data.table")
library(data.table)
bs <- blocks(s)   # block layout
acc <- data.table(zone_id = integer(), code = integer(), n = numeric())
for (i in seq_len(nrow(bs))) {
m <- readValues(s, row = bs$row[i], nrows = bs$nrows[i], mat = TRUE)
# m has two columns: zone_id, code
# drop NAs
keep <- !is.na(m[,1]) & !is.na(m[,2])
if (!any(keep)) next
m <- m[keep, , drop = FALSE]
# count combinations in this block
dt <- as.data.table(m)
setnames(dt, c("zone_id", "code"))
dt[, n := .N, by = .(zone_id, code)]
dt <- unique(dt[, .(zone_id, code, n)])
# accumulate
acc <- rbindlist(list(acc, dt), use.names = TRUE)
print(i)
}
s
bs
nr <- nrow(s)
nr
# choose chunk size in rows.
# Bigger chunks = faster but more RAM. 1000–5000 is usually safe.
chunk_rows <- 2000L
chunk_rows
rows  <- seq.int(1L, nr, by = chunk_rows)
nrows <- pmin(chunk_rows, nr - rows + 1L)
# accumulate results as a list (MUCH faster than rbind in a loop)
out_list <- vector("list", length(rows))
for (i in seq_along(rows)) {
m <- readValues(s, row = rows[i], nrows = nrows[i], mat = TRUE)
# ensure numeric/integer (some setups may return factors for categorical)
z <- m[, 1]
c <- m[, 2]
keep <- !is.na(z) & !is.na(c)
if (!any(keep)) next
dt <- data.table(
zone_id = as.integer(z[keep]),
code    = as.integer(c[keep])
)
# counts of (zone_id, code) in this chunk
out_list[[i]] <- dt[, .(count = .N), by = .(zone_id, code)]
if (i %% 10 == 0) message("chunk ", i, "/", length(rows))
}
s <- c(zone_r, crop_type_bav)
names(s) <- c("zone_id", "code")
nr <- nrow(s)
chunk_rows <- 2000L
rows  <- seq.int(1L, nr, by = chunk_rows)
nrows <- pmin(chunk_rows, nr - rows + 1L)
out_list <- vector("list", length(rows))
# IMPORTANT: open files for reading
readStart(s)
on.exit(readStop(s), add = TRUE)
for (i in seq_along(rows)) {
m <- readValues(s, row = rows[i], nrows = nrows[i], mat = TRUE)
z <- m[, 1]
c <- m[, 2]
keep <- !is.na(z) & !is.na(c)
if (!any(keep)) next
dt <- data.table(
zone_id = as.integer(z[keep]),
code    = as.integer(c[keep])
)
out_list[[i]] <- dt[, .(count = .N), by = .(zone_id, code)]
if (i %% 10 == 0) message("chunk ", i, "/", length(rows))
}
s <- c(zone_r, crop_type_bav)
names(s) <- c("zone_id", "code")
nr <- nrow(s)
chunk_rows <- 2000L
rows  <- seq.int(1L, nr, by = chunk_rows)
nrows <- pmin(chunk_rows, nr - rows + 1L)
out_list <- vector("list", length(rows))
# IMPORTANT: open files for reading
readStart(s)
on.exit(readStop(s), add = TRUE)
for (i in seq_along(rows)) {
m <- readValues(s, row = rows[i], nrows = nrows[i], mat = TRUE)
z <- m[, 1]
c <- m[, 2]
keep <- !is.na(z) & !is.na(c)
if (!any(keep)) next
dt <- data.table(
zone_id = as.integer(z[keep]),
code    = as.integer(c[keep])
)
out_list[[i]] <- dt[, .(count = .N), by = .(zone_id, code)]
if (i %% 10 == 0) message("chunk ", i, "/", length(rows))
print(i)
}
seq_along(rows)
library(terra)
library(sf)
library(tidyverse)
library(giscoR)
library(data.table)
nuts1 <- gisco_get_nuts(year = 2021,  resolution = "10", nuts_level = 1, epsg = 4326)
bavaria_sf <- subset(nuts1, NUTS_ID == "DE2")
bavaria_v  <- vect(bavaria_sf)
# gosif raster
gosif_202405 <- rast('../data/enhanced_sif/gosif/GOSIF_2024.M05.tif')
# necessary transformations to correct sif value's range
gosif_202405[gosif_202405 >= 32766] <- NA
gosif_202405 <- gosif_202405 * 0.0001
gosif_bav_crop <- crop(gosif_202405, bavaria_v)
gosif_bav_crop <- mask(gosif_bav_crop, bavaria_v)
# raster has different fields in germany marked, colored (winter wheat, maize, soyabean etc.)
crop_type <- rast('../data/crop_type_tif/croptypes_2024.tif')
# set levels for crop type raster
crop_classes <- readr::read_delim("../data/crop_type_tif/LEGEND_CropTypes.txt", delim = "\t")
colnames(crop_classes) <- c("code", "label")
levels(crop_type) <- data.frame(value = crop_classes$code, crop = crop_classes$label)
bavaria_utm <- project(bavaria_v, "EPSG:32632")
# Crop the Germany-wide crop raster down to Bavaria first
crop_type_bav <- crop(crop_type, bavaria_utm)
crop_type_bav <- mask(crop_type_bav, bavaria_utm)
# Project the (already Bavaria-masked) SIF raster to UTM
# (continuous values -> bilinear is appropriate)
gosif_bav_utm <- project(gosif_bav_crop, "EPSG:32632", method = "bilinear")
# (optional) ensure it’s still strictly within Bavaria border after reprojection
gosif_bav_utm <- mask(gosif_bav_utm, bavaria_utm)
# make each sif pixel into polygon
zones <- as.polygons(gosif_bav_utm, dissolve = FALSE, values = TRUE, na.rm = TRUE)
zones$zone_id <- 1:nrow(zones)
zone_r <- rasterize(zones, crop_type_bav, field = "zone_id", touches = TRUE)
compareGeom(zone_r, crop_type_bav, stopOnError = TRUE)
s <- c(zone_r, crop_type_bav)
names(s) <- c("zone_id", "code")
nr <- nrow(s)
chunk_rows <- 2000L
rows  <- seq.int(1L, nr, by = chunk_rows)
nrows <- pmin(chunk_rows, nr - rows + 1L)
out_list <- vector("list", length(rows))
# IMPORTANT: open files for reading
readStart(s)
on.exit(readStop(s), add = TRUE)
seq_along(rows)
for (i in seq_along(rows)) {
m <- readValues(s, row = rows[i], nrows = nrows[i], mat = TRUE)
z <- m[, 1]
c <- m[, 2]
keep <- !is.na(z) & !is.na(c)
if (!any(keep)) next
dt <- data.table(
zone_id = as.integer(z[keep]),
code    = as.integer(c[keep])
)
out_list[[i]] <- dt[, .(count = .N), by = .(zone_id, code)]
if (i %% 10 == 0) message("chunk ", i, "/", length(rows))
print(i)
}
tab <- rbindlist(out_list, use.names = TRUE, fill = TRUE)
tab <- tab[, .(count = sum(count)), by = .(zone_id, code)]
setorder(tab, zone_id, -count)
head(tab)
legend_df <- levels(crop_type)[[1]]  # columns: value, crop
legend_df
comp <- dplyr::as_tibble(tab) %>%
dplyr::group_by(zone_id) %>%
dplyr::mutate(
total_cells = sum(count),
pct = count / total_cells
) %>%
dplyr::ungroup() %>%
dplyr::left_join(legend_df, by = c("code" = "value"))
comp %>%
group_by(zone_id) %>%
summarise(sum_pct = sum(pct)) %>%
summary()
ww_zones <- comp %>%
filter(code == 11, pct >= 0.20) %>%
pull(zone_id) %>%
unique()
length(ww_zones)
comp_ww <- comp %>%
filter(zone_id %in% ww_zones) %>%
arrange(zone_id, desc(pct))
comp_ww
comp_ww_summary <- comp_ww %>%
mutate(pct_txt = paste0(round(pct * 100, 1), "% ", crop)) %>%
group_by(zone_id) %>%
summarise(composition = paste(pct_txt, collapse = ", "), .groups = "drop")
comp_ww_summary
comp_ww
ww_zones <- comp %>%
dplyr::filter(code == 11, pct >= 0.20) %>%
dplyr::pull(zone_id) %>%
dplyr::unique()
?unique
?pull
?filter
ww_zones <- comp %>%
dplyr::filter(code == 11, pct >= 0.20) %>%
dplyr::pull(zone_id) %>%
unique()
length(ww_zones)
ww_zones <- comp %>%
dplyr::filter(code == 11, pct >= 0.05) %>%
dplyr::pull(zone_id) %>%
unique()
length(ww_zones)
ww_zones <- comp %>%
dplyr::filter(code == 11, pct >= 0) %>%
dplyr::pull(zone_id) %>%
unique()
length(ww_zones)
ww_zones <- comp %>%
dplyr::filter(code == 11, pct >= 0.1) %>%
dplyr::pull(zone_id) %>%
unique()
length(ww_zones)
comp
comp
comp %>%
group_by(zone_id) %>%
summarise(sum_pct = sum(pct)) %>%
summary()
legend_df
# Compute “vegetated total” per zone (exclude code 0)
veg_totals <- comp %>%
filter(code != 0) %>%           # exclude no_data
group_by(zone_id) %>%
summarise(veg_cells = sum(count), .groups = "drop")
veg_totals
# Compute winter wheat share among vegetation
ww_share_veg <- comp %>%
filter(code == 11) %>%           # winter wheat
select(zone_id, ww_cells = count) %>%
left_join(veg_totals, by = "zone_id") %>%
mutate(ww_pct_veg = ww_cells / veg_cells)
ww_share_veg
# Compute winter wheat share among vegetation
ww_share_veg <- comp %>%
dplyr::filter(code == 11) %>%           # winter wheat
dplyr::select(zone_id, ww_cells = count) %>%
dplyr::left_join(veg_totals, by = "zone_id") %>%
dplyr::mutate(ww_pct_veg = ww_cells / veg_cells)
ww_share_veg
ww_zones <- ww_share_veg %>%
dplyr::filter(ww_pct_veg >= 0.20) %>%
dplyr::pull(zone_id)
ww_zones
View(ww_share_veg)
length(ww_zones)
ww_zones <- ww_share_veg %>%
dplyr::filter(ww_pct_veg >= 0.20) %>%
dplyr::pull(zone_id)
length(ww_zones)
ww_zones
ww_share_veg
comp
ww_share_veg
comp <- comp %>%
left_join(
ww_share_veg %>% select(zone_id, ww_pct_veg),
by = "zone_id"
)
comp
comp_ww
comp_ww_all <- comp %>%
dplyr::filter(zone_id %in% ww_zones) %>%
dplyr::arrange(zone_id, dplyr::desc(pct))
comp_ww_all
comp
comp
is.na(ww_pct_veg)
ww_zones <- ww_share_veg %>%
dplyr::filter(!is.na(ww_pct_veg), ww_pct_veg >= 0.20) %>%
dplyr::pull(zone_id)
length(ww_zones)
comp <- comp %>%
left_join(
ww_share_veg %>% select(zone_id, ww_pct_veg),
by = "zone_id"
)
comp
comp <- comp %>%
select(-ww_pct_veg.x, -ww_pct_veg.y)
comp <- comp %>%
left_join(
ww_share_veg %>% select(zone_id, ww_pct_veg),
by = "zone_id"
)
comp
zones_ww <- zones[zones$zone_id %in% ww_zones, ]
zones_ww
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥20% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
ww_zones <- ww_share_veg %>%
dplyr::filter(!is.na(ww_pct_veg), ww_pct_veg >= 0) %>%
dplyr::pull(zone_id)
length(ww_zones)
zones_ww <- zones[zones$zone_id %in% ww_zones, ]
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥20% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
ww_zones <- ww_share_veg %>%
dplyr::filter(!is.na(ww_pct_veg), ww_pct_veg >= 0.1) %>%
dplyr::pull(zone_id)
length(ww_zones)
zones_ww <- zones[zones$zone_id %in% ww_zones, ]
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥20% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
length(ww_zones)
nrow(zones_ww)
# 1) Download NUTS3 in EPSG:4326 (lightweight) and filter to Bavaria (DE2...)
nuts3 <- gisco_get_nuts(year = 2021, resolution = "10", nuts_level = 3, epsg = 4326)
bav_nuts3_sf <- nuts3[grepl("^DE2", nuts3$NUTS_ID), ]
# 2) Convert to terra vector + project to UTM 32N
bav_nuts3_v <- terra::vect(bav_nuts3_sf)
bav_nuts3_utm <- terra::project(bav_nuts3_v, "EPSG:32632")
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
lines(bav_nuts3_utm, col = "blue", lwd = 0.8)
lines(bav_nuts3_utm, col = "blue", lwd = 1.8)
?gisco_get_nuts
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
lines(bav_nuts3_utm, col = "blue", lwd = 1.8)
ww_zones <- ww_share_veg %>%
dplyr::filter(!is.na(ww_pct_veg), ww_pct_veg >= 0.05) %>%
dplyr::pull(zone_id)
zones_ww <- zones[zones$zone_id %in% ww_zones, ]
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
lines(bav_nuts3_utm, col = "blue", lwd = 1.8)
# Convert to terra vector + project to UTM
bav_nuts3_v <- terra::vect(bav_nuts3_sf)
bav_nuts3_v <- terra::project(bav_nuts3_v, "EPSG:32632")
zones_ww <- zones[zones$zone_id %in% ww_zones, ]
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥10% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
lines(bav_nuts3_v, col = "blue", lwd = 1.8)
length(ww_zones)
# Plot SIF background + outline qualifying pixels
plot(gosif_bav_utm, main = "GOSIF May 2024 (Bavaria) – pixels with ≥5% winter wheat")
lines(zones, col = "grey60", lwd = 0.3)      # optional: show all pixel boundaries faintly
lines(zones_ww, col = "red", lwd = 1.8)      # highlight qualifying pixels
lines(bav_nuts3_v, col = "blue", lwd = 1.8)
df <- readRDS('../data/oco2_sif.rds')
library(sf)
library(tidyverse)
library(lubridate)
df_2024 <- df |> dplyr::filter(year(Delta_Time) == 2024)
df_2024
st_area(df_2024)
df_2024 |> st_make_valid() |> st_area()
df_2024$total_area_m2/1e+6
sum(df_2024$total_area_m2/1e+6)
View(df_2024)
colnames(df_2024)
plot(df_2024["Daily_SIF_740nm"])
